import { PlaceholderPage } from "./placeholder";

export function CleanupPage() {
  return <PlaceholderPage title="Cleanup" description="Cleanup previews will start read-only before any destructive operations are introduced." />;
}
