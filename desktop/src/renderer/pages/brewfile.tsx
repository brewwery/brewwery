import { PlaceholderPage } from "./placeholder";

export function BrewfilePage() {
  return <PlaceholderPage title="Brewfile" description="Brewfile import and export will be explicit, local, and reviewable." />;
}
